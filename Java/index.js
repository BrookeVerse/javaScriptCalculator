//Listening to which button is pressed, what type of button was pressed

const calculator = document.querySelector(".calcuArea");
const keys = calculator.querySelector(".calcuArea__buttons");

keys.addEventListener("click", (e) => {
  if (e.target.matches("button")) {
  }
});
